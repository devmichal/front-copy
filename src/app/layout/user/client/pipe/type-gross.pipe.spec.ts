import { TypeGrossPipe } from './type-gross.pipe';

describe('TypeGrossPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeGrossPipe();
    expect(pipe).toBeTruthy();
  });
});
